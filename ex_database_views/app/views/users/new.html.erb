<!DOCTYPE html>

<h3>Create New User</h3>

<%= form_for User.new do |f| %>
	<%= f.label :fname %>
	<%= f.text_field :fname %><br>
	<%= f.label :lname %>
	<%= f.text_field :lname %><br>
	<%= f.submit :create %>
<% end %>